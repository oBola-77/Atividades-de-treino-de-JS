let letra;
 letra = prompt("Digite uma letra: ");

 if(letra === "a" || letra === "e" || letra === "i" || letra === "o" || letra === "u" || letra === "A" || letra === "E" || letra === "I" || letra === "O" || letra === "U"){
    alert("Esta letra é uma volgal");
 }
 else{
    alert("Esta letra é uma consoante");
 }